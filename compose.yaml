services:
  emdash:
    build: .
    ports:
      - "4321:4321"
    environment:
      - EMDASH_SITE_URL=https://emdash.testedeli.online
    volumes:
      - emdash-data:/app/data
      - emdash-uploads:/app/uploads
    restart: unless-stopped

volumes:
  emdash-data:
  emdash-uploads:
